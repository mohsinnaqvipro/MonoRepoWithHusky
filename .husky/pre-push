echo "🔧 Building backend and frontend using Nx..."

# Run builds in strict mode
npx nx run-many --target=build --projects=backend,frontend --parallel --strict

# Check for build failure
if [ $? -ne 0 ]; then
  echo "❌ Build failed. Push aborted."
  exit 1
fi

echo "✅ Build successful. Proceeding with push..."
