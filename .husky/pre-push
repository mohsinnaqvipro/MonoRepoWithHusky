echo "🔧 Building backend and frontend using Nx..."

# Build both apps in parallel and fail if any fails
npx nx run-many --target=build --projects=backend,frontend --parallel

if [ $? -ne 0 ]; then
  echo "❌ Build failed. Push aborted."
  exit 1
fi

echo "✅ Build successful. Proceeding with push..."
