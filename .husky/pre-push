echo "🔍 Detecting locally affected projects..."

npx nx affected --target=build --parallel --base=HEAD

if [ $? -ne 0 ]; then
  echo "❌ Build failed. Push aborted."
  exit 1
fi

echo "✅ Build successful. Proceeding with push..."
